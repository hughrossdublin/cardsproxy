apply plugin: "war"

bootWar {
    mainClassName = "ie.aib.dbbe.cards.proxy.CardsproxyApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
